Place approved public or organizational source documents in this folder before running `python scripts/ingest_kb.py`.

Supported formats: PDF, TXT, Markdown, HTML.

The current `data/kb_records.json` remains the runnable assessment demo dataset. Do not claim it is official policy material.
