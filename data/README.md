# Data provenance

`kb_records.json` is an assessment demo dataset created to make the local application runnable. It is not an official insurance policy and must not be represented as one.

For the final submission, replace or augment these records with approved public or organizational documents and preserve their URLs, page numbers, retrieval date, and version in the record metadata.
